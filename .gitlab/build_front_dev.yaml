build_front_prod:
  stage: build
  extends: .build_front_template
  # variables:
  #   APP_ENV: ${CI_PROD_APP_ENV}
  only:
    refs:
      - dev
  # except:
  #   changes:
  #     - .docker/docs/**/*

build_front_prod_manual:
  stage: build
  extends: .build_front_template
  # variables:
  #   APP_ENV: ${CI_PROD_APP_ENV}
  when: manual
  only:
    refs:
      - dev
