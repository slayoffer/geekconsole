build_dev:
  stage: build
  extends: .build_template
  # variables:
  #   APP_ENV: ${CI_PROD_APP_ENV}
  only:
    refs:
      - dev
  # except:
  #   changes:
  #     - .docker/docs/**/*

build_dev_manual:
  stage: build
  extends: .build_template
  # variables:
  #   APP_ENV: ${CI_PROD_APP_ENV}
  when: manual
  only:
    refs:
      - dev
