version: '3.9'

services:
  geekconsole:
    container_name: geekconsole-develop
    build:
      context: .
      target: build
      dockerfile: ./Dockerfile
      # no_cache: true
    restart: unless-stopped
    ports:
      - 3000:3000
